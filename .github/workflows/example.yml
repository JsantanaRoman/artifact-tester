name: example

on:
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build image
        run: |
          docker build -t test_image:latest -<<EOF
          FROM busybox
          RUN touch abc.txt
          EOF
